FRONTEND_PORT=5173

# Dev proxy target — override to point at a remote backend (e.g. staging)
# Defaults to http://localhost:BACKEND_PORT if not set
BACKEND_PORT=3000
# BACKEND_URL=https://api.staging.example.com

# Environment
NODE_ENV=development